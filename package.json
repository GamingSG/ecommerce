{
  "name": "ecommerce-app",
  "version": "1.0.0",
  "description": "Full-stack MERN e-commerce application",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "install-all": "npm install && cd client && npm install --omit=optional && cd ../server && npm install",
    "build": "cd client && npm install --omit=optional && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
